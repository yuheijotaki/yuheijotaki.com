---
title: 'WordPress + Nuxt.js でポートフォリオサイトを作る その5'
description: ''
pubDate: 01/15/2020 20:00:00
---

WordPressサイト（<a href="http://works.yuheijotaki.com/">http://works.yuheijotaki.com/</a>）の REST API と Nuxt.js でポートフォリオサイトを作成する。その5

<span itemscope itemtype="http://schema.org/Photograph"><img src="/images/hatena/20190924093802.png" alt="f:id:jotaki:20190924093802p:plain" title="f:id:jotaki:20190924093802p:plain" class="hatena-fotolife" itemprop="image"></span>

GitHub: <a href="https://github.com/yuheijotaki/works-nuxt">https://github.com/yuheijotaki/works-nuxt</a>  
Netlify: <a href="https://works-yuheijotaki.netlify.com/">https://works-yuheijotaki.netlify.com/</a>

<h2>Pug と Nuxt.js(Vue.js)</h2>

はじめてpug使ってみたのでちょっととまどったところ  
属性名と属性値はぜんぶ <code>()</code> の中に突っ込めばOKという感じですね

<h3><code>&lt;NuxtLink&gt;</code></h3>

html

```html
<p><NuxtLink :to="`/work/${item.slug}/`">detail</NuxtLink></p>
```

pug

```html
p
  nuxt-link(:to="`/work/${item.slug}/`") detail
```

<h3><code>&lt;childComponent&gt;</code></h3>

html

```html
<ul>
  <ChildComponent
    v-for="(post,index) in posts"
    :item="post"
    :key="index"
  >
</ul>
```

pug

```html
ul
  ChildComponent(
    v-for="(post,index) in posts"
    :item="post"
    :key="index"
    )
  )
```

<h3><code>:src (v-bind:src)</code></h3>

html

```html
<img :src="image.url" alt="title">
```

```html
img(
  :src="image.url"
  :alt="title"
)
```

<h3><code>@click (v-on:click)</code></h3>

html

```html
<li>
  <a
    @click="clickedEvent('hoge')"
  >Hoge</a>
</li>
```

pug

```html
li
  a(
    @click="clickedEvent('hoge')"
  ) Hoge
```

